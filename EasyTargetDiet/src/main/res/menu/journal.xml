<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:etdiet="http://schemas.android.com/apk/res-auto" >

    <item android:id="@+id/quick_add"
        android:icon="@android:drawable/ic_menu_add"
        android:title="@string/quick_add"
        etdiet:showAsAction="ifRoom" />

    <item android:id="@+id/weight"
        android:title="@string/weight"
        etdiet:showAsAction="never" />

    <item android:id="@+id/settings"
        android:title="@string/settings"
        etdiet:showAsAction="never" />

</menu>