<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.arndroid.etdiet"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:theme="@style/Theme.AppCompat.Light"
            android:name=".journal.JournalAct"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat.Light"
            android:name=".foodsusage.FoodsUsageAct"
            android:label="@string/app_name"
            android:parentActivityName=".journal.JournalAct">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".journal.JournalAct" />
        </activity>

        <provider android:name=".provider.Provider"
            android:authorities="br.com.arndroid.provider.etdiet"
            android:exported="false" />

        <service android:name=".virtualweek.JournalService" />

    </application>

</manifest>